---
import DownloadLayout from "../layouts/DownloadLayout.astro"
import { DownloadList, DownloadListItem } from "../components"

const title = "Typefind panel"
const description =
  "Typefind panel – in-line playlist search bar for Columns UI"
const repo = "typefind_panel"
---

<DownloadLayout title={title} metaDescription={description} repo={repo}>
  <p>Typefind is an in-line playlist search bar for Columns UI.</p>

  <h3 class="title is-3">Downloads</h3>
  <h4 class="title is-4">Latest version</h4>
  <DownloadList>
    <DownloadListItem
      filename="foo_uie_typefind-0.3.1.fb2k-component"
      label="Version 0.3.1"
    />
  </DownloadList>
</DownloadLayout>
